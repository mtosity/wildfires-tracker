{
  "buildCommand": "npx vite build --config vite.config.ts && mkdir -p public && cp -r client/dist/* public/ && cp -r client/public/* public/",
  "outputDirectory": "./public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}